[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/twenty-docker/twenty/Dockerfile.railway"

[deploy]
# Keep the image entrypoint behavior (DB setup/migrations) and run API.
startCommand = "node dist/main"
healthcheckPath = "/railway-healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
