[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/twenty-docker/twenty-front/Dockerfile.railway"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
